<Window x:Class="CursorBubble.Settings.AiScriptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Genereer script met AI"
        Width="620" Height="600"
        MinWidth="560" MinHeight="520"
        WindowStartupLocation="CenterOwner"
        Background="#1B1F27"
        Icon="pack://application:,,,/Assets/app.ico"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="PanelText" Color="#EAEEF6" />
        <SolidColorBrush x:Key="SubText" Color="#9AA4B4" />
        <SolidColorBrush x:Key="CardBorder" Color="#33FFFFFF" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource PanelText}" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="Background" Value="#2A0E1116" />
            <Setter Property="Foreground" Value="{StaticResource PanelText}" />
            <Setter Property="CaretBrush" Value="{StaticResource PanelText}" />
            <Setter Property="BorderBrush" Value="{StaticResource CardBorder}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="14,7" />
            <Setter Property="Margin" Value="6,0,0,0" />
            <Setter Property="Foreground" Value="{StaticResource PanelText}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" CornerRadius="6" Background="#1EFFFFFF"
                                BorderBrush="{StaticResource CardBorder}" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#33FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Beschrijf wat het script moet doen"
                   FontWeight="SemiBold" Margin="0,0,0,6" />
        <TextBox Grid.Row="1" x:Name="DescriptionBox" Height="70"
                 TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
            <Button x:Name="GenerateBtn" Content="Genereren" Click="GenerateBtn_Click"
                    Background="#4C8DFF" Margin="0" />
            <TextBlock x:Name="StatusText" Foreground="{StaticResource SubText}"
                       VerticalAlignment="Center" Margin="12,0,0,0" TextWrapping="Wrap" />
        </StackPanel>

        <StackPanel Grid.Row="3" x:Name="ResultInfo" Visibility="Collapsed" Margin="0,14,0,0">
            <TextBlock Text="Uitleg" FontWeight="SemiBold" Margin="0,0,0,3" />
            <TextBlock x:Name="ExplanationText" Foreground="{StaticResource SubText}" TextWrapping="Wrap" />
            <Border x:Name="WarningBox" Visibility="Collapsed" Margin="0,8,0,0"
                    Background="#3A2A1400" BorderBrush="#66E0A000" BorderThickness="1"
                    CornerRadius="6" Padding="10">
                <TextBlock x:Name="WarningsText" Foreground="#FFD98A" TextWrapping="Wrap" />
            </Border>
        </StackPanel>

        <DockPanel Grid.Row="4" x:Name="ScriptArea" Visibility="Collapsed" Margin="0,12,0,0">
            <TextBlock DockPanel.Dock="Top" Margin="0,0,0,3"
                       Text="Script — lees dit door voordat je het gebruikt:" FontWeight="SemiBold" />
            <TextBox x:Name="ScriptBox" FontFamily="Consolas, Cascadia Mono, Courier New"
                     TextWrapping="NoWrap" AcceptsReturn="True" AcceptsTab="True"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" />
        </DockPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button x:Name="CancelBtn" Content="Annuleren" Click="CancelBtn_Click" Margin="0" />
            <Button x:Name="UseBtn" Content="Gebruiken" Click="UseBtn_Click"
                    IsEnabled="False" Background="#4C8DFF" />
        </StackPanel>
    </Grid>
</Window>
